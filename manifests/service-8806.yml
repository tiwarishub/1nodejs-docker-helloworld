apiVersion: v1
kind: Service
metadata:
    name: "aaaaatiwarishubaks-775b"
    labels:
        app: "aaaaatiwarishubaks-775b"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "aaaaatiwarishubaks-775b"