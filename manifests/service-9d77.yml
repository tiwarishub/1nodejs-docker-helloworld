apiVersion: v1
kind: Service
metadata:
    name: "aaaaatiwarishubaks-c160"
    labels:
        app: "aaaaatiwarishubaks-c160"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "aaaaatiwarishubaks-c160"