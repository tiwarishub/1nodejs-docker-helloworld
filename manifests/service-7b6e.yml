apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-754a"
    labels:
        app: "diaggarw-754a"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-754a"