apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-f9d3"
    labels:
        app: "diaggarw-f9d3"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-f9d3"
