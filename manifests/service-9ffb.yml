apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-d114"
    labels:
        app: "diaggarw-d114"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-d114"