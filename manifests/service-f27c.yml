apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-619b"
    labels:
        app: "diaggarw-619b"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-619b"
