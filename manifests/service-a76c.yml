apiVersion: v1
kind: Service
metadata:
    name: "aaaaatiwarishubaks-b612"
    labels:
        app: "aaaaatiwarishubaks-b612"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "aaaaatiwarishubaks-b612"
